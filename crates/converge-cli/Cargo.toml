[package]
name = "converge-cli"
version.workspace = true
edition.workspace = true

[[bin]]
name = "converge"
path = "src/main.rs"

[dependencies]
anyhow.workspace = true
clap.workspace = true
converge-client.workspace = true
converge-model.workspace = true
serde.workspace = true
serde_json.workspace = true
time.workspace = true

[dev-dependencies]
# The resolve-loop e2e drives the real binary against a real server: the
# flow it proves is exactly the one a user follows (roadmap 016).
axum.workspace = true
converge-server.workspace = true
tokio.workspace = true
tempfile.workspace = true
